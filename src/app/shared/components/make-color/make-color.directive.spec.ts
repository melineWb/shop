import { MakeColorDirective } from './make-color.directive';

describe('MakeColorDirective', () => {
  it('should create an instance', () => {
    const directive = new MakeColorDirective();
    expect(directive).toBeTruthy();
  });
});
